# SSL/TLS Certificate Lifecycle Management - Frontend

React admin dashboard with pages for login, dashboard, certificates, alerts, internal certificates, and settings.

## Setup

```bash
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8000`.
